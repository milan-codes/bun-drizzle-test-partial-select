# Drizzle ORM bug test

See [#954](https://github.com/drizzle-team/drizzle-orm/issues/954) on Drizzle ORM.

Install dependencies:

```bash
bun install
```

Push database schema:

```bash
bun push
```

Seed the database:

```bash
bun seed
```

Run `index.ts`:

```bash
bun start
```
